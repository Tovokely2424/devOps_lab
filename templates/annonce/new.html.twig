{% extends "base.html.twig" %}

{% block title %} - Creation d'une annonce
{% endblock %}

{#{% form_theme form _self %}'partial/theme.html.twig' a la place du self#}
{% block body %}
<div class="container">
    <h1 class="text-center">Création d'une nouvelle annonce</h1>
    <div class="w-75 mx-auto">
        
        {{ form_start(form) }}
            {{ form_widget(form)}}
        {{ form_end(form)}}
    </div>
</div>

{% endblock %}
{#
{% block form_row %}
  dump()
    <div class="form-group">
        {{ form_label(form)}}
        {{ form_widget(form)}}
    </div>
{% endblock %}
{% block text_widget %}
    <input type="text" name="{{ full_name}}" id="{{ id}}" class="form-control">
{% endblock %}

#}